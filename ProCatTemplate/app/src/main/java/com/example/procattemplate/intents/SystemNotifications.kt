package com.example.procattemplate.intents

/**
 * Тут в формате обычных строк объявляются имена сигналов.
 * Важно, чтобы строковое значение каждого сигнала было уникальным.
 */
object SystemNotifications {

    const val gotUserDataIntent = "got-user-data"
    const val myTestIntent = "test-intent"

}